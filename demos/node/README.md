# jstinder node example

To get started, copy `settings.example.json` to `settings.json` and set your facebook ID and token from [this](http://opentinder.bitballoon.com/).

Run `node index.js` to see the results.